# Server
server.port=${PORT:8080}

# PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/college_marketplace}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Aishwarya1@}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secretKey=${JWT_SECRET:your_super_secret_key_make_it_long_and_random_123456789}

spring.main.allow-circular-references=true

# Image upload limits
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Frontend URL (used for CORS)
app.frontend-url=${FRONTEND_URL:http://localhost:5173}

# Cloudinary
cloudinary.cloud-name=dxbhvzysj
cloudinary.api-key=547836876616559
cloudinary.api-secret=gY4CWD7kAPtkZWi6eLkgW-vvjql

server.forward-headers-strategy=framework

spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000


#brevo mail api
brevo.api.key=${BREVO_API_KEY}

spring.application.name=college-marketplace
